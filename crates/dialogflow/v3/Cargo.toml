[dependencies]
serde_json = "1"

[dependencies.serde]
features = ["derive"]
version = "1"

[package]
authors = ["Ezra Celli <ezra@ezracelli.com>"]
description = "Builds conversational interfaces (for example, chatbots, and voice-powered apps and devices)."
edition = "2018"
name = "google_api_rust_client_dialogflow_v3"
version = "0.1.0+20210219"
